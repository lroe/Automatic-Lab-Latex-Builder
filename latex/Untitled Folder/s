\documentclass{article}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{tikz}

\geometry{margin=1in}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[R]{\thepage}

% Define standard border using tikz
\AtBeginDocument{%
  \AddToShipoutPictureBG{%
    \begin{tikzpicture}[remember picture,overlay]
      \draw[line width=1pt] ($(current page.north west)+(40pt,-40pt)$) rectangle ($(current page.south east)-(40pt,-40pt)$);
    \end{tikzpicture}%
  }%
}

\begin{document}

\section*{Question 1: Insert data in the created database.}
\begin{verbatim}
INSERT INTO STAFF (StaffID, StfFirstName, StfLastName, StfStreetAddress, StfCity, StfState, StiZipCode, StfAreaCode, StfPhoneNumber, DateHired, Salary, Position)
    VALUES (4, 'Mary', 'Johnson', '567 Pine St', 'Mumbai', 'Maharashtra', 400001, 22, '9876541230', '2019-03-25', 32000.00, 'Associate Professor');

% Continue with the rest of the INSERT statements...
\end{verbatim}

% Repeat the same structure for other questions...

\end{document}
